# Book-Tale — Documentation Folder Cleanup & De-LLM-ification Audit (2026-08-15)

## 1. Executive Summary

Scope: full `docs/` tree — root docs, `community/`, `decisions/`, `design/`,
`product/`, `project/`, `reference/`, `technical/`, `assets/` (runbooks,
agents), `migration/`. The folder reads as **human-curated, not
LLM-scaffolded**: it has dated ADRs (0001–0010) with real rationale, decision
records referencing actual code, a postmortem, runbooks, and migration records
with specific dates. One stale duplicate was removed; the only remaining
LLM-flavored artifact is a deliberately archived agent prompt awaiting an owner
decision.

## 2. Urgent: Leaked Secrets/Credentials Found

None. No keys, tokens, or internal URLs in any docs file.

## 3. LLM/AI Fingerprints Removed

None in active documentation. All "generated by" / "placeholder" matches are
legitimate technical references (e.g. `placeholder` in migration-runbook
contexts, config placeholder values documented in ADRs).

## 4. Structural Changes

- **Removed** `docs/reference/folder_structure.md` — stale pre-restructure
  duplicate of the canonical `docs/folder_structure.md`. Last touched in the
  original docs commit (1587027); the root copy is referenced by
  `migration/migration_summary.md` and `migration/file_move_ledger.md` and
  carries the authoritative tree + change log. Zero inbound links to the
  `reference/` copy. `git rm` (recoverable from history).

## 5. Duplicate Content Consolidated

- `folder_structure.md` (root) vs `reference/folder_structure.md` — the two
  had diverged (the root copy documents the v5.0 restructure; the reference
  copy described the older pre-modernization tree). Canonical file: root
  `docs/folder_structure.md`. Stale copy removed (see §4).

## 6. Contradictions Found (manual review, not auto-resolved)

None found between live docs.

## 7. Boilerplate/Template Cruft Removed

None — `community/` (CHANGELOG, CODE_OF_CONDUCT, CONTRIBUTING, SECURITY,
SUPPORT) is populated with project-specific content, and the ADR set is
genuine.

## 8. Dead Links Fixed/Removed

None — all internal links resolve; no `example.com`/placeholder external links
in docs.

## 9. README / CONTRIBUTING / CONSTITUTION Review

- No `docs/README.md` index — top-level `architecture.md` +
  `folder_structure.md` serve as entry points; acceptable for this layout.
- `community/CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, `SECURITY.md` reflect
  real contribution/security posture (no template org-name placeholders).

## 10. Security/Privacy Findings

None. Secrets are referenced as config keys only, never pasted.

## 11. Consistency Fixes Applied

- File-layout consistency: removed the one duplicated doc (see §4).

## 12. Files Modified

- `docs/README.md` — none (no index file exists).
- `docs/audit/cleanup-audit-2026-08-15.md` — added (this report).

## 13. Files/Folders Deleted

- `docs/reference/folder_structure.md` (stale duplicate; `git rm`)

## 14. Remaining Manual Review Items

1. **RESOLVED (2026-08-15):** `docs/assets/agents/AGENTS.md` + `.cursorrules`
   were **deleted**. Decision rationale: the archived `AGENTS.md` is a
   generic "UNIVERSAL ULTRA MASTER PROMPT" with zero project-specific
   content and zero consumers (no code/CI/Docker refs); the analysis_report
   recommendation to "promote like sibling repos" was based on a false
   premise — no sibling repo has a root `AGENTS.md`. The repo's real agent
   instructions live in `.github/copilot-instructions.md`. `.dockerignore`
   exclusions for both files were dropped; `folder_structure.md`, the docs
   index, and the CHANGELOG were updated.

## 15. "Does This Still Look AI-Scaffolded?" Score

**99 / 100** — 100 baseline; −1 for the (now resolved) archived agent-prompt
artifact, recorded here for traceability. No empty topic folders, no
contradictions, no tone inconsistency, no fabricated stats; ADRs and runbooks
carry real dates and specifics.
